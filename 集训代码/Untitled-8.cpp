#include <iostream>

using namespace std;

int check(int a){
    int sum;

    return sum;
}

int main(){
    int n;
    while(scanf("%d",&n)!=EOF){
        cin>>a;
        if(a==0) break;
        int sum;
        sum=check(a);
        cout<<sum<<endl;
    }
    return 0;
}
