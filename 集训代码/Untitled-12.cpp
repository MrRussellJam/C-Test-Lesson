#include <iostream>
#include <string.h>

using namespace std;

int number[1000];
int sum;
int num;
string
bool istail;


bool dfs(int ste){
    if()
}

int main(){

    while (1){
        memset(number,0,sizeof(number));
        cin>>sum>>num;
        istail = true;
        if(sum == 0 && num == 0) break;
        for(int i = 0;i < num; i++){
            cin >> number[i];
        }
        dfs(0);
    }
    return 0;
}